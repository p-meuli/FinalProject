using UnityEngine;

public class Debt : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
//Final Debt should be 70,000 
// public int debt = 70,000;

// Collect GEM, Substract from the debt variable

// loop: collect gem, determine type of gem, subtract from debt variable
// If green = 1
// if blue = 5
// if red = 20
// if purple = 100
// if Topaz = 559
// if white = 1443

// Jerome Powell --> Jerome Sporewell